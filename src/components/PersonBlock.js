import styled from 'styled-components';

const PersonBlock = styled.section`
    position: relative;
    border-radius: 10px;
    margin: 0 auto;
    width: 30%;
    min-height: 600px;
    background: #fff;
`;

export default PersonBlock
